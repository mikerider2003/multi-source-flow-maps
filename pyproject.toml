[project]
name = "code"
version = "0.1.0"
description = "Multi-Source Flow Maps Visualization"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "geopandas",
    "matplotlib",
    "folium",
    "plotly",
    "scipy",
]
